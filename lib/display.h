#include "states.h"

class Display {       // The class
  public:             // Access specifier
    State state ;        // Attribute (int variable)
    string myString;  // Attribute (string variable)
};